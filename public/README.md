# Public Assets

This directory contains static assets that will be served when running the web version.

The `data/` folder from the root will be copied here during the build process so the dashboard can fetch JSON files.
